import { CustDircDirective } from './cust-dirc.directive';

describe('CustDircDirective', () => {
  it('should create an instance', () => {
    const directive = new CustDircDirective();
    expect(directive).toBeTruthy();
  });
});
